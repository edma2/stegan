stegan.py
Author: Eugene Ma (edma2)

Dependencies: 
----------------------
PyCrypto (https://www.dlitz.net/software/pycrypto/)
PIL (http://www.pythonware.com/products/pil/)

Description:
----------------------
pystego is a steganographic library for python, which encodes arbitrary streams
of bytes into PNG image files.

Usage:
----------------------
>>> image = Image.open('tux.png').convert('RGB')
>>> stegan.encode(image, 'password', 'hello, world!')
>>> image.save("test.png")
>>> image = Image.open('test.png').convert('RGB')
>>> stegan.decode(image, 'password')
'hello, world!'
>>>
